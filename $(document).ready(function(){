$(document).ready(function(){
	$("#search").on("change",search);

})

function search(){
	//get value #search
	//pass value into AJAX/GET & handle results

	//if search is empty, reload contents of page
}

as soon as they click inside the search save the entire page as a variable

make sure they send a valid picture
